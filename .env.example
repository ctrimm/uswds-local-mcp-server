# ============================================
# USWDS MCP Server - Environment Variables
# ============================================
#
# This file shows all available environment variables for the USWDS MCP Server.
#
# IMPORTANT: This server is designed to run via Claude Desktop, where environment
# variables are set in the Claude Desktop config file (claude_desktop_config.json).
# You typically do NOT need a .env file for normal use.
#
# This file is provided for:
# - Testing the server outside of Claude Desktop
# - Understanding available configuration options
# - Development and debugging
#
# To use this file:
# 1. Copy this file to .env (git will ignore it)
# 2. Uncomment and set the variables you need
# 3. Run the server with: node dist/index.js
#
# ============================================

# --------------------------------------------
# Component Mode
# --------------------------------------------
# Controls whether the server provides React-USWDS or vanilla USWDS components.
#
# Values:
#   - "false" = Vanilla USWDS (HTML/CSS)
#   - "true"  = React-USWDS (React components from @trussworks/react-uswds)
#
# Default: "false"
#
# Note: When using Claude Desktop, set this in claude_desktop_config.json instead:
#
# {
#   "mcpServers": {
#     "uswds": {
#       "command": "/path/to/start-mcp-server.sh",
#       "env": {
#         "USE_REACT_COMPONENTS": "false"
#       }
#     }
#   }
# }
#
USE_REACT_COMPONENTS=false

# --------------------------------------------
# Development Settings (Optional)
# --------------------------------------------

# Log level
LOG_LEVEL=info

# --------------------------------------------
# Email Configuration (Resend)
# --------------------------------------------
# Email service for sending welcome emails and API key notifications
#
# To enable email:
# 1. Sign up for Resend at https://resend.com
# 2. Add and verify your sending domain (e.g., mail.uswdsmcp.com)
# 3. Get your API key from the Resend dashboard
# 4. Set the secret via SST: npx sst secret set RESEND_API_KEY "re_..."
#
# Email sender address (must be verified in Resend)
EMAIL_FROM=USWDS MCP <noreply@mail.uswdsmcp.com>

# Enable/disable email sending
EMAIL_ENABLED=true

# --------------------------------------------
# Cloudflare Configuration (Optional)
# --------------------------------------------
# For custom domain DNS management
#
# To use Cloudflare DNS:
# 1. Log in to Cloudflare and select your domain
# 2. Go to "My Profile" > "API Tokens"
# 3. Create a token with "Zone:DNS:Edit" permissions
# 4. Set the secret via SST: npx sst secret set CLOUDFLARE_API_TOKEN "your-token"
#
# Note: CLOUDFLARE_ZONE_ID is set in sst.config.ts or via SST env
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token-here
CLOUDFLARE_ZONE_ID=your-cloudflare-zone-id-here

# --------------------------------------------
# AWS/SST Secrets (Set via SST CLI)
# --------------------------------------------
# These are managed by SST and should be set via:
#   npx sst secret set SECRET_NAME "value" --stage production
#
# Required secrets:
# - RESEND_API_KEY: Resend API key for sending emails
# - CLOUDFLARE_API_TOKEN: Cloudflare API token for DNS (optional)
#
# Example:
#   npx sst secret set RESEND_API_KEY "re_abc123..." --stage production
#   npx sst secret set CLOUDFLARE_API_TOKEN "your-token" --stage production

# ============================================
# End of Configuration
# ============================================
#
# For more information:
# - README.md - Project overview
# - SETUP.md - Installation guide
# - QUICK_START.md - Quick start guide
# - MCP_TOOLS_GUIDE.md - Tool documentation
#
